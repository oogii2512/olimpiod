#include <iostream>
#include <iomanip>
#include <cmath>
#include <cstdlib>
#include <ctime>
#define PI 3.141592
#include <bits/stdc++.h>
using namespace std;
int gcd(int a, int b){
	return a==0 ? b :(b%a,a);
}
int lcm(int a, int b){ return a/ gcd(a, b)*b; 
}
int main(){
	float a, b,c,d,e,f,g,h,i,j,k,1; 
	cin >> a >> b >> c >> d >> e >> f >> g >> h >> i >> j >> k >> 1;
	cout << fixed << setprecision(3)<<
	abs(a-b)<<endl<<sqrt((g-j)* (g-j)+(h-k)*(i-1)*(i-1));
	return 0;
}


